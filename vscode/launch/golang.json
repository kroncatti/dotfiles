{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "env": {
                "PORT": ":8080",
                "LOG_LEVEL": "DEBUG",
                "MONGO_URI": "mongodb://admin:password@localhost:27017"
              }
        }
    ]
}